<section id="sidebar">
    <a href="#" class="brand">
        <i class='bx bxs-cog'></i>
        <span class="text">Admin Hub</span>
    </a>
    <ul class="side-menu top">
        <li class="active">
            <a asp-controller="Admin" asp-action="Index">
                <i class='bx bxs-dashboard'></i>
                <span class="text">Dashboard</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="UserReport">
                <i class='bx bxs-graduation'></i>
                <span class="text">User Report</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="AddStaff">
                <i class='bx bx-book-bookmark'></i>
                <span class="text">Add Staff</span>
            </a>
        </li>
        <li>
            <a asp-controller="Admin" asp-action="LectureReport">
                <i class='bx bx-qr'></i>
                <span class="text">Lecture Report</span>
            </a>
        </li>
    </ul>
    <ul class="side-menu">
        <li>
            <a asp-controller="Admin" asp-action="Profile" class="profile">
                <i class='bx bxs-user'></i>
                <span class="text">Profile</span>
            </a>
        </li>
        <li>
            <a asp-controller="Student" asp-action="logout" class="logout">
                <i class='bx bxs-log-out-circle'></i>
                <span class="text">Logout</span>
            </a>
        </li>
    </ul>
</section>
<section id="content">
    <nav>
        <i class='bx bx-menu'></i>
        <div class="send-right">
            <div class="send-right-box">
                <input type="checkbox" id="switch-mode" hidden>
                <label for="switch-mode" class="switch-mode"></label>
                <a asp-controller="Staff" asp-action="Profile" class="profile">
                    <div class="p-0 m-0">
                        <h6>Admin</h6>
                        
                    </div>
                </a>
            </div>
        </div>
    </nav>
    <script>
        const isDarkMode = localStorage.getItem('darkMode') === 'true';
        if (isDarkMode) {
            document.body.classList.add('dark');
        }

        document.addEventListener('DOMContentLoaded', () => {
            const switchMode = document.getElementById('switch-mode');

            // Ensure the switch reflects the current state
            if (switchMode) {
                switchMode.checked = isDarkMode;

                // Listen for changes and toggle the dark mode class
                switchMode.addEventListener('change', function () {
                    if (this.checked) {
                        document.body.classList.add('dark');
                        localStorage.setItem('darkMode', 'true');
                    } else {
                        document.body.classList.remove('dark');
                        localStorage.setItem('darkMode', 'false');
                    }
                });
            }
        });</script>
